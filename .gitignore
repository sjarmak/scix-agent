# Data files (large, not in repo)
*.jsonl
*.jsonl.gz
*.jsonl.xz
# ...except small, hand-curated gold-standard evals checked in with code.
!eval/claim_extraction_gold_standard.jsonl
ads_metadata_by_year_picard/
ads_metadata_by_year_full/
data/*
# ...except small, checked-in evaluation artifacts that track with code.
# Do NOT put credentials / env dumps under data/eval/*.csv|yaml — this
# carve-out has no way to distinguish calibration data from secrets.
!data/eval/
data/eval/*
!data/eval/*.csv
!data/eval/*.yaml
!data/eval/*.md
!data/eval/*.jsonl
!data/eval/.gitkeep
# Nested gold-set subdirs — carved out explicitly so the shallow
# data/eval/* block above does not swallow them.
!data/eval/entity_value_props/
!data/eval/entity_value_props/*.yaml
!data/eval/entity_value_props/*.md
# Test eval fixtures (small, hand-authored, track with code).
!tests/eval/
!tests/eval/*.jsonl

# Logs
*.log
logs/

# Secrets & environment
.env
.env.*
!.env.example
.beads-credential-key

# Model weights & checkpoints
models/

# Build / agent artifacts
build-artifacts/
*.deb

# Internal development docs
CLAUDE.md
results/
docs/AGENTS.md
docs/prd/
docs/premortem/
docs/briefing.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.coverage
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Issue tracking & agent orchestration
.beads/
.dolt/
*.db
.brainstorm/
.gc/
.codex/

# MCP config (machine-specific paths)
.mcp.json

# Claude Code ephemeral state
.claude/rfc-dag.state.json
.claude/rfc-dag.log.md
.claude/rfc-dag-artifacts/
.claude/prd-build-artifacts/
.claude/worktrees/
.claude/prd-build.state.json

# Cache
.cache/
deploy/.env
CLAUDE.local.md
.claude/scheduled_tasks.lock

# Qdrant pilot container volume (local docker mount)
.qdrant_storage/

# prd-build orchestration artifacts
.prd-build/
