# M4.5 — Lane Delta (Wikidata-backfill unreachable entities)

One row per entity that exists in the static lane via Wikidata backfill but is structurally unreachable from the JIT lane's candidate derivation.

> u12 status: the lane_delta_set is a stub (empty). The real set will land with u07 Wikidata backfill; the arithmetic path already subtracts this set from the numerator and denominator of the adjusted Jaccard.

| bibcode | entity_id | reason |
|---------|-----------|--------|
| _(none)_ | _(none)_ | _(none — stub returns empty set)_ |

